import React from 'react';
import { View, Text } from 'react-native';

import Styles from './Styles';

export default props => (
    <View style={Styles.tabContent}>
        <Text>Tab de Contacts</Text>
    </View>    
)